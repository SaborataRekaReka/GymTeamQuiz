{
  "name": "saboratarekareka",
  "version": "0.1.0",
  "private": true,
  "description": "Chatium quiz funnel for Usmanova Fit test task. Source, agent instructions and implementation notes.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint:content": "node scripts/check-content-rules.mjs",
    "check": "npm run typecheck && npm run lint:content",
    "preview:vue": "vite --config chatium/dev/vite.config.ts",
    "build:assets:one-folder": "node scripts/build-assets-one-folder.mjs",
    "build:assets:by-screen": "node scripts/build-assets-by-screen.mjs"
  },
  "devDependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "@vitejs/plugin-vue": "^5.1.0",
    "sharp": "^0.35.2",
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vue": "^3.4.0"
  }
}
